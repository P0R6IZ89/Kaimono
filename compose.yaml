services:
  db:
    image: postgres:15-alpine
    container_name: kaimono
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: dev-db
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
